# Dockerfile.node
FROM node:18-slim
WORKDIR /app
COPY package*.json ./
# This command installs dependencies from package-lock.json for consistency
RUN npm ci --production
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]

