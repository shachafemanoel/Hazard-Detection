[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.united"

[deploy]
startCommand = "/app/scripts/start-united.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.kv]
PORT = "8080"
NODE_ENV = "production"
